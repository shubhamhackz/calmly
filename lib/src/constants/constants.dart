enum ThemeSetting { system, dark, light }

enum Breathe {
  inhale,
  holdBreathe,
  exhale,
  idle,
}

enum CalmBox {
  expand,
  shrink,
  busy,
  completedExpand,
  completedShrink,
  cancel,
  stop,
}
